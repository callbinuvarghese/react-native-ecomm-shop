# Express Server Configuration

# Database connection string (required)
DATABASE_URL=postgresql://user:password@host/database

# Server port (default: 3000)
PORT=3000

# Server host/IP address
# Use 0.0.0.0 to listen on all network interfaces (allows access from other devices)
# Use 127.0.0.1 or localhost to only allow local connections
# Use a specific IP like 192.168.1.18 to bind to that network interface
HOST=0.0.0.0

# Environment mode
NODE_ENV=development
